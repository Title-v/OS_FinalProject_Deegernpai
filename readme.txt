Tutorial(for MacOS)
install this library in the terminal:
pip install streamlit pygame psutil matplotlib pydub numpy

copy the filepath of the code example:
/Users/kai/Desktop/OS-final/unoptimize_comment.py

How to execute :
Put this command in the terminal:
streamlit run {filepath}

But of you use vscode make sure that you open the vscode by the anaconda navigation
and open the folder that have code and copy filepath of the code and execute by this command:
streamlit run {filepath}

Tutorial(for window)
1.open anaconda prompt and install this:
pip install streamlit pygame psutil matplotlib pydub numpy
2.copy the filepath of the code example:
/Users/kai/Desktop/OS-final/unoptimize_comment.py
3. execute by put this command in the anaconda prompt:
streamlit run {filepath}
